These files are needed to work with git on KDE git repositories.
Edit the file to your needs or use these git calls:
---
$ git config --global user.name "Your Name"
$ git config --global user.email your@email
---

